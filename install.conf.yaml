- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.aliases: aliases/aliases
    ~/.aliases.docker: aliases/aliases.docker
    ~/.vimrc:
    ~/.gitconfig: git/gitconfig
    ~/.gitconfig.mw: git/gitconfig.mw
    ~/.gitconfig.personal: git/gitconfig.personal
    ~/.aliases.mathworks: mathworks/aliases.mathworks
    ~/.fish_env:
    ~/.iterm2_shell_integration.fish: functions/iterm2_shell_integration.fish
    ~/.rc: bash_scripts/rc

- create:
  - ~/.config/fish

- shell:
    # - chsh -s $(which sh)
    - cp -r ./functions ~/.config/functions/

