---
- import_tasks: tasks/mac/install_brew_packages.yml
  when: run_brew_tasks
  tags: ['brew', 'packages']

- import_tasks: tasks/mac/configure_dotfiles.yml
  when: run_brew_tasks
  tags: ['brew', 'packages']
